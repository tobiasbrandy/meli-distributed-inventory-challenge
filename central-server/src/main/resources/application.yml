server:
  port: 8081

spring:
  config:
    import: "optional:classpath:messaging.yml"

  datasource:
    url: jdbc:h2:mem:inventorydb;DB_CLOSE_DELAY=-1
    driverClassName: org.h2.Driver
    username: user
    password:

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false

app:
  stores:
    - store-1
